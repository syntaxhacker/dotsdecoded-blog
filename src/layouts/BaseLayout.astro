---
const {
  title = 'DotsDecoded',
  description = 'Deep dives into systems, tools, and the code that powers them.',
  image = '/og-default.svg',
  type = 'website',
  canonicalURL,
} = Astro.props
import '../styles/global.css'

const siteUrl = Astro.site ? new URL(Astro.site).origin : 'https://dotsdecoded-blog.pages.dev'
const url = canonicalURL ? new URL(canonicalURL, siteUrl).href : Astro.url.href
const ogImage = new URL(image, siteUrl).href
---
<!doctype html>
<html lang="en" data-testid="html-root">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content={description} />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css" />
  <link rel="canonical" href={url} data-testid="canonical-link" />
  <title data-testid="page-title">{title}</title>

  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content={type} />
  <meta property="og:url" content={url} />
  <meta property="og:image" content={ogImage} />
  <meta property="og:site_name" content="DotsDecoded" />
  <meta property="og:locale" content="en_US" />

  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={ogImage} />
  <script is:inline>
    const t = localStorage.getItem('theme')
    if (t) document.documentElement.setAttribute('data-theme', t)
    else if (window.matchMedia('(prefers-color-scheme: light)').matches)
      document.documentElement.setAttribute('data-theme', 'light')

    window.__toggleTheme = function() {
      const c = document.documentElement.getAttribute('data-theme')
      const n = c === 'light' ? 'dark' : 'light'
      document.documentElement.setAttribute('data-theme', n)
      localStorage.setItem('theme', n)
    }
  </script>
  <script is:inline>
    document.addEventListener('DOMContentLoaded', function() {
      var nav = document.getElementById('toc-nav');
      if (!nav) return;
      var links = nav.querySelectorAll('.toc-link');
      if (links.length === 0) return;
      var ids = [];
      links.forEach(function(l) {
        var el = document.getElementById(l.dataset.target);
        if (el) ids.push(el);
      });
      function onScroll() {
        var active = links[0] ? links[0].dataset.target : '';
        for (var i = 0; i < ids.length; i++) {
          if (ids[i].getBoundingClientRect().top <= 32) active = links[i].dataset.target;
        }
        for (var i = 0; i < links.length; i++) {
          if (links[i].dataset.target === active) links[i].classList.add('toc-active');
          else links[i].classList.remove('toc-active');
        }
      }
      var scrollEl = document.querySelector('.page-inner') || window;
      scrollEl.addEventListener('scroll', onScroll, { passive: true });
      onScroll();
    });
  </script>
  <script is:inline>
    document.addEventListener('DOMContentLoaded', function() {
      document.querySelectorAll('.prose pre').forEach(function(pre) {
        var btn = document.createElement('button');
        btn.className = 'copy-btn';
        btn.textContent = 'copy';
        btn.addEventListener('click', function() {
          var code = pre.querySelector('code');
          if (!code) return;
          navigator.clipboard.writeText(code.textContent).then(function() {
            btn.textContent = 'copied';
            setTimeout(function() { btn.textContent = 'copy'; }, 1500);
          });
        });
        pre.appendChild(btn);
      });
    });
  </script>
  <script is:inline>
    document.addEventListener('DOMContentLoaded', function() {
      var path = window.location.pathname;
      if (!path.startsWith('/blog/')) return;
      
      var slug = path.replace('/blog/', '');
      var markedKey = 'markedAsRead_' + slug;
      
      if (sessionStorage.getItem(markedKey)) return;
      
      function getProgress() {
        try {
          var data = localStorage.getItem('blogProgress');
          return data ? JSON.parse(data) : {};
        } catch (e) {
          return {};
        }
      }
      
      function setProgress(progress) {
        try {
          localStorage.setItem('blogProgress', JSON.stringify(progress));
        } catch (e) {}
      }
      
      var progress = getProgress();
      if (!progress[slug]) {
        progress[slug] = { read: false, completed: false };
      }
      progress[slug].lastVisited = new Date().toISOString();
      setProgress(progress);
      
      var scrollEl = document.querySelector('.page-inner') || window;
      
      function onScroll() {
        if (sessionStorage.getItem(markedKey)) return;
        
        var scrollTop = scrollEl === window ? window.pageYOffset : scrollEl.scrollTop;
        var scrollHeight = scrollEl === window ? document.documentElement.scrollHeight : scrollEl.scrollHeight;
        var clientHeight = scrollEl === window ? window.innerHeight : scrollEl.clientHeight;
        
        if (clientHeight >= scrollHeight) return;
        
        var scrollPercent = scrollTop / (scrollHeight - clientHeight);
        
        if (scrollPercent >= 0.5) {
          sessionStorage.setItem(markedKey, 'true');
          var prog = getProgress();
          if (!prog[slug]) {
            prog[slug] = { read: false, completed: false };
          }
          prog[slug].read = true;
          prog[slug].lastVisited = new Date().toISOString();
          setProgress(prog);
          scrollEl.removeEventListener('scroll', onScroll);
        }
      }
      
      scrollEl.addEventListener('scroll', onScroll, { passive: true });
    });
  </script>
  <script>
    import Prism from 'prismjs'
    import 'prismjs/components/prism-bash'
    import 'prismjs/components/prism-c'
    import 'prismjs/components/prism-cpp'
    import 'prismjs/components/prism-graphql'
    import 'prismjs/components/prism-javascript'
    import 'prismjs/components/prism-json'
    import 'prismjs/components/prism-protobuf'
    import 'prismjs/components/prism-typescript'
    import 'prismjs/components/prism-python'
    import 'prismjs/components/prism-ruby'
    import 'prismjs/components/prism-go'
    import 'prismjs/components/prism-sql'
    Prism.highlightAll()
  </script>
</head>
<body>
  <slot />
</body>
</html>
